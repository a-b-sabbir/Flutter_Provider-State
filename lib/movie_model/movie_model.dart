// the movies will be saved in a list and we want to save them as an object,
// that's why we created this model file.

class Movie {
  final String title;
  final String? duration;

  Movie({required this.title, this.duration});

}
